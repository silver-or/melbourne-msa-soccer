module.exports = {
  server: {
    port: 8000,
    host: '0.0.0.0',
    timing: false
  }
}

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = nextConfig